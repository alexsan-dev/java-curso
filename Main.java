public class Main {
  public static void main(String[] args) {
    // STRING | INT | BOOLEAN | CHAR | DOUBLE
    int edad = 20;
    int constante = 2;

    // OPERACIONES
    // suma +
    // resta -
    // multiplicacion *
    // division /
    System.out.println((edad + constante + (edad * constante) - (constante - edad)) * constante);
  }
}
